# Python

The Python standatd module provides a strong native integration with Python based on pythonnet or CPython implementation.

The use of Python programming language will be one of the main extension methods for developing in Pure.

## Issues

* `print` will not work because pythonnet didn't implement it.'